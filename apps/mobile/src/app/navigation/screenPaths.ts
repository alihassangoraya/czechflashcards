import type { Screen } from "../appTypes";

export const screenPaths: Record<Screen, string> = {
  home: "/",
  login: "/login",
  progress: "/progress",
  quiz: "/quiz",
  register: "/register",
  study: "/flashcards"
};
