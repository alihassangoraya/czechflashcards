export type AccountStudySummary = {
  deckTotal: number;
  studiedCount: number;
  masteredCount: number;
  learningCount: number;
  dueCount: number;
  customCount: number;
  savedCount: number;
  examLevel: string;
  syncStatus: string;
};
