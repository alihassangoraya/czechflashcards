import { StyleSheet } from "react-native";
import { colors } from "../../../theme/design";

export const progressDashboardScreenStyles = StyleSheet.create({
  screen: { flex: 1, backgroundColor: colors.background },
  scroll: { flex: 1 }
});
