import { StyleSheet } from "react-native";
import { spacing } from "../../../theme/design";

export const studyCardRelatedWordsStyles = StyleSheet.create({
  related: { alignSelf: "stretch", gap: spacing.md, marginTop: spacing.xs }
});
