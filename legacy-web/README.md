# Legacy Web Reference

This folder contains the pre-React static implementation kept for migration
reference only. The production web app is built from `apps/mobile` and deployed
from `apps/mobile/dist`.

Do not add new product behavior here. Move reusable behavior into
`packages/shared` or the React Native app instead.
